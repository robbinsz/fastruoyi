# Agent Distribution Tasks

## Completed In This Slice
- 补充代理分销 SDD 文档，固定目标、分层和推进顺序。
- 扩展 `sys_user` 模型，加入代理身份字段。
- 新增代理分销领域 ORM 模型。
- 新增超管接口：分配 L1、授权创建次级、撤销授权、查看代理树。
- 新增 `agent_scope_service`，作为后续数据隔离实现基础。
- 新增代理管理接口：直属下级代理列表、直属客户列表、创建下级代理、创建直属客户。
- 新增投注核心接口：创建链接、打开链接、确认投注、确认开奖结果、我的投注记录。
- 新增报表接口：看板汇总、收益明细、趋势、直属下级汇总、我的收益、导出。
- 新增 MySQL 初始化 SQL 草案，覆盖角色、菜单、字段、业务表和默认梯度数据。
- 对齐联调细节：修正投注管理页面测试路由，补齐目录级角色菜单授权，并放宽 API smoke test 到“路由存在且响应结构正确”。
- 新增 20 个分销测试账号初始化脚本，并补充分销流程集成测试与环境变量说明。
- 已按技术文档修正提成梯度模型，引入 `commission_amt` 并将收益计算改为固定提成额分账。
- 已新增提成配置接口与前端配置页，并补齐报表看板 Redis 缓存、导出文件命名和 5000 条导出上限。
- 已补齐代理详情、代理启停、代理提成系数修改接口。

## Next Tasks
- 在真实数据库执行初始化 SQL，验证角色、菜单、权限和目录展示。
- 安装前端依赖后执行构建与页面联调，修正接口字段或动态菜单细节。
- 补集成测试，覆盖多账号、投注闭环、数据隔离和报表一致性。

## Acceptance For This Slice
- 超管接口已具备最小可用后端实现。
- 代理基础管理接口已具备最小可用后端实现。
- 投注核心闭环后端已具备最小可用实现。
- 报表核心查询接口已具备最小可用实现。
- 新表模型可以被 SQLAlchemy 自动建表识别。
- 代理域基础代码已通过 `python -m compileall` 语法校验，Ruff 待环境安装后补跑。
